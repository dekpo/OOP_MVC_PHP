<?php

/**
 * Il peut s'avérer pratique de récupérer ici $data
 * Et éventuellement de déclarer des variables de traitement
 * */
$errors = $data['errors'];
$userAdded = $data['userAdded'];
?><h1 class="text-center">Registration</h1>
<?php if (!empty($errors)) : ?>
  <div class="alert alert-danger" role="alert">
    <?php foreach ($errors as $error) : ?>
      <p><?= $error ?></p>
    <?php endforeach; ?>
  </div>
<?php endif; ?>
<?php if (!$userAdded): ?>
<form method="POST" id="registration_form">
    <div class="mb-3">
        <label for="title" class="form-label">Name</label>
        <input type="text" class="form-control" name="name" id="name" placeholder="Name">
    </div>
    <div class="mb-3">
        <label for="description" class="form-label">Email</label>
        <input type="text" class="form-control" name="email" id="email" placeholder="Email">
    </div>
    <div class="mb-3">
        <label for="src" class="form-label">Password</label>
        <input type="password" class="form-control" name="password" id="password" placeholder="Password">
    </div>
    <div class="mb-3">
        <label for="confirm_password" class="form-label">Confirm Password </label>
        <input type="password" class="form-control" name="confirm_password" id="confirm_password" placeholder="Confirm Password">
    </div>
    <div class="mb-3">
        <input type="button" class="btn btn-primary" name="registration_btn" id="registration_btn" value="Register">
    </div>
</form>
<?php else: ?>
    <div class="alert alert-success" role="alert">
        Compte ajouté avec succès !!! Vous pouvez vous <a href="?page=login">connecter</a> desormais.
    </div>
<?php endif; ?>
<script>
    const password = document.getElementById("password");
    const confirmPassword = document.getElementById("confirm_password");
    const registerBtn = document.getElementById("registration_btn");
    const registerForm = document.getElementById("registration_form");

    if(registerBtn !== null){
    registerBtn.addEventListener("click", () => {
        if (password.value != "" && password.value != confirmPassword.value) {
            alert("Password & Confirm password do not match !");
            return false;
        } 
        registerForm.submit();
    });
}
</script>